spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

ticket:
  invoice:
    policy:
      keywords:
        - "pr"
        - "-pr"
        - "ir"
        - "-ir"
        - "peru rail"
        - "perurail"
        - "inca rail"
        - "incarail"
    validation:
      enabled: true

google:
  drive:
    application-name: spring-drive-app
    credentials:
      location: classpath:credentials/pupe-advance-credential.json
    scopes:
      - https://www.googleapis.com/auth/drive.readonly

logging:
  level:
    root: INFO
    com.christiancanari.pupe: DEBUG
    org.apache.pdfbox: WARN
    com.google.api.client: WARN
    com.google.api.client.http: WARN
    com.google.auth: WARN
